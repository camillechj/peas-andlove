### Install the packages into library on compute canada

install.packages("readr", dependencies=TRUE, INSTALL_opts='--no-lock')

install.packages("dplyr", dependencies=TRUE, INSTALL_opts='--no-lock')

install.packages("stringr", dependencies=TRUE, INSTALL_opts='--no-lock')

install.packages("openxlsx", dependencies=TRUE, INSTALL_opts='--no-lock')

install.packages("tidyverse", dependencies=TRUE, INSTALL_opts='--no-lock')
